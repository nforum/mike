node_modules
.next
dist
out
build
.open-next

.env
.env.*
!.env.example
!.env.local.example

# MCP server config — may contain secrets; track the .example instead
mike/mcp.json
!mike/mcp.json.example

# Staged copy of mike/mcp.json that the deploy scripts drop into the
# backend Docker build context. Always rebuilt by cloudbuild.yaml /
# scripts/deploy.sh; never commit it.
backend/mike/

*.log
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
coverage

vanjske_datoteke/

# Word add-in production bundle is emitted by `cd word-addin && npm run build`
# directly into the frontend's public folder. Treat it as a build artifact —
# rebuild it on every deploy.
frontend/public/word-addin/
